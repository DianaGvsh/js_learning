// 1. Create object with product field equal to 'iphone'
const device = {
  product: 'iphone',
};
console.log(device);

// 2. Add a price field equal to 1000 and a currency field equal to 'dollar' to the object
device.price = 1000;
device.currency = 'dollar';

// 3. Add a details field which will contain an object with model and color fields
device.details = {
  model: '',
  color: '',
};
